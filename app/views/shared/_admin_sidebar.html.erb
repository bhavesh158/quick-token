<aside class="hidden lg:flex lg:w-72 lg:h-screen lg:sticky lg:top-0 shrink-0 flex-col border-r border-slate-200 bg-white">
  <div class="border-b border-slate-200 px-6 py-5">
    <%= link_to quicktoken_brand_path, class: "flex items-center gap-3" do %>
      <div class="flex h-9 w-9 items-center justify-center rounded-lg bg-primary text-white">
        <span class="material-symbols-outlined text-xl">confirmation_number</span>
      </div>
      <div>
        <p class="text-lg font-bold tracking-tight text-slate-900">QuickToken</p>
        <p class="text-xs uppercase tracking-wider text-slate-500">Admin</p>
      </div>
    <% end %>
  </div>

  <nav class="flex-1 px-4 py-5">
    <p class="mb-3 px-2 text-xs font-semibold uppercase tracking-wide text-slate-500">Navigation</p>
    <div class="space-y-2">
      <a href="<%= admin_token_queues_path %>" class="block rounded-lg px-3 py-2 text-sm font-semibold <%= active_item == :queues ? 'bg-slate-900 text-white' : 'text-slate-700 hover:bg-slate-100' %>">
        Queues
      </a>
      <div class="block rounded-lg border border-dashed border-slate-300 px-3 py-2 text-sm font-semibold text-slate-400">Analytics (Coming Soon)</div>
      <div class="block rounded-lg border border-dashed border-slate-300 px-3 py-2 text-sm font-semibold text-slate-400">Settings (Coming Soon)</div>
    </div>
  </nav>

  <div class="border-t border-slate-200 p-4">
    <%= form_with(url: admin_logout_path, method: :delete) do |form| %>
      <%= form.submit "Logout", class: "w-full rounded-lg bg-slate-900 px-3 py-2 text-sm font-bold text-white hover:bg-slate-700" %>
    <% end %>
  </div>
</aside>
